{
  "name": "mobiapp",
  "version": "1.0.0",
  "private": true,
  "description": "Lightweight Cloud Run service (deploy only this folder)",
  "scripts": {
    "build": "node --check server.js",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.0.1"
  }
}
